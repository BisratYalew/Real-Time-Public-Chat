$(function(){
    var socket = io.connect();
    var $messageForm = $('#messageForm');
    var $message = $('#message');
    var $chat = $('#chatWindow');
    var $usernameForm = $("#UsernameForm");
    var $users = $('#users');
    var $username = $('#username');
    var $error = $('#error');
    
})